create a keypad with react
blank screen on top 
keypad 
1  2  3
4  5  6
7  8  9
←  0  ✓
as key in a number
puts the number on the top blank screen
back button erases the last entry
valid button clears content

try if 'hello'.length = 4 returns 'hell'

bonus

onClick change colors
onClick valid button, tell if code is valid or error
